[tox]
# Ensure 1.6+ is used to support 'skipsdist'
minversion = 1.6
skipsdist = True
envlist = flake8,py27

[testenv]
commands = nosetests {posargs}
deps = -rtest-requirements.txt

[testenv:flake8]
commands = flake8
deps = flake8

[testenv:dib]
setenv = DIB_DEBIAN_USE_DEBOOTSTRAP_CACHE=1
commands = {toxinidir}/dib/tests.sh
deps = -rdib/requirements.txt
