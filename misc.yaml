- project:
    name: 'VisualEditor'
    jobs:
     - '{name}-jslint'
     - '{name}-npm'
     - '{name}-ruby1.9.3lint'

- project:
    name: 'oojs-core'
    jobs:
     - '{name}-jslint'
     - '{name}-npm'

- project:
    name: 'oojs-ui'
    jobs:
     - '{name}-jslint'
     - '{name}-npm'
