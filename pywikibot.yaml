- project:
    name: pywikibot-compat

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    name: pywikibot-core

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    name: pywikibot-i18n

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'
