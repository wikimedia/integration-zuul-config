- hosts: all
  name: Autoconverted job legacy-php-compile-php72-docker from old job php-compile-php72-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
