- hosts: all
  name: Autoconverted job legacy-pywikibot-FLOSSbot-tox-docker from old job pywikibot-FLOSSbot-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
