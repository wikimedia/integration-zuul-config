- hosts: all
  name: Autoconverted job legacy-quibble-vendor-sqlite-php72-docker from old job quibble-vendor-sqlite-php72-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
