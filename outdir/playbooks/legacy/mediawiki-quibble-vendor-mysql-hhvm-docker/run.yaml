- hosts: all
  name: Autoconverted job legacy-mediawiki-quibble-vendor-mysql-hhvm-docker from old
    job mediawiki-quibble-vendor-mysql-hhvm-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
