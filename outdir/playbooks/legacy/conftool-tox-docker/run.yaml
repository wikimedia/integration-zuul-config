- hosts: all
  name: Autoconverted job legacy-conftool-tox-docker from old job conftool-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
