- hosts: all
  name: Autoconverted job legacy-cumin-tox-docker from old job cumin-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
