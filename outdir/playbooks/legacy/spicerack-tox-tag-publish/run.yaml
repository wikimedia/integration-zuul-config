- hosts: all
  name: Autoconverted job legacy-spicerack-tox-tag-publish from old job spicerack-tox-tag-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
