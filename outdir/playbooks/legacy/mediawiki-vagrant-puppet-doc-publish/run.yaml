- hosts: all
  name: Autoconverted job legacy-mediawiki-vagrant-puppet-doc-publish from old job
    mediawiki-vagrant-puppet-doc-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
