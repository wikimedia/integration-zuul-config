- hosts: all
  name: Autoconverted job legacy-mwext-php70-phan-docker from old job mwext-php70-phan-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
