- hosts: all
  name: Autoconverted job legacy-poolcounter-tox-docker from old job poolcounter-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
