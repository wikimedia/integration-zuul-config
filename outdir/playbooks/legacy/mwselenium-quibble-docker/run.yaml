- hosts: all
  name: Autoconverted job legacy-mwselenium-quibble-docker from old job mwselenium-quibble-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
