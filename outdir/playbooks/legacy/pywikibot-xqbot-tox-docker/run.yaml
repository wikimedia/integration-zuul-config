- hosts: all
  name: Autoconverted job legacy-pywikibot-xqbot-tox-docker from old job pywikibot-xqbot-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
