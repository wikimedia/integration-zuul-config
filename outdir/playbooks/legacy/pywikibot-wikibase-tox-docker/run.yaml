- hosts: all
  name: Autoconverted job legacy-pywikibot-wikibase-tox-docker from old job pywikibot-wikibase-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
