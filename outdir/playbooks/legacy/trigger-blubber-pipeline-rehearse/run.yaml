- hosts: all
  name: Autoconverted job legacy-trigger-blubber-pipeline-rehearse from old job trigger-blubber-pipeline-rehearse
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
