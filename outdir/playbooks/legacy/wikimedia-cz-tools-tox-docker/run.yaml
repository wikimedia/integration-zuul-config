- hosts: all
  name: Autoconverted job legacy-wikimedia-cz-tools-tox-docker from old job wikimedia-cz-tools-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
