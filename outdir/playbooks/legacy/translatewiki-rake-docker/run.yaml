- hosts: all
  name: Autoconverted job legacy-translatewiki-rake-docker from old job translatewiki-rake-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
