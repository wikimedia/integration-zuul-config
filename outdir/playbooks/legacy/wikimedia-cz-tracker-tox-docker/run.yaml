- hosts: all
  name: Autoconverted job legacy-wikimedia-cz-tracker-tox-docker from old job wikimedia-cz-tracker-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
