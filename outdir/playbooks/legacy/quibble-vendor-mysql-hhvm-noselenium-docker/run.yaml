- hosts: all
  name: Autoconverted job legacy-quibble-vendor-mysql-hhvm-noselenium-docker from
    old job quibble-vendor-mysql-hhvm-noselenium-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
