- hosts: all
  name: Autoconverted job legacy-operations-mw-config-hhvm-composer-test-docker from
    old job operations-mw-config-hhvm-composer-test-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
