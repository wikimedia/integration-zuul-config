- hosts: all
  name: Autoconverted job legacy-composer-hhvm-docker from old job composer-hhvm-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
