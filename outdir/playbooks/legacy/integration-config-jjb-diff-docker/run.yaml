- hosts: all
  name: Autoconverted job legacy-integration-config-jjb-diff-docker from old job integration-config-jjb-diff-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
