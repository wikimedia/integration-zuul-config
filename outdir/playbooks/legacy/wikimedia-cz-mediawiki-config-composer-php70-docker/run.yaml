- hosts: all
  name: Autoconverted job legacy-wikimedia-cz-mediawiki-config-composer-php70-docker
    from old job wikimedia-cz-mediawiki-config-composer-php70-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
