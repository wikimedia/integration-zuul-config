- hosts: all
  name: Autoconverted job legacy-lintr-docker-non-voting from old job lintr-docker-non-voting
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
