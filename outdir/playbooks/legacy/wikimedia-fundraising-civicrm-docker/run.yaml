- hosts: all
  name: Autoconverted job legacy-wikimedia-fundraising-civicrm-docker from old job
    wikimedia-fundraising-civicrm-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
