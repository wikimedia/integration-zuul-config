- hosts: all
  name: Autoconverted job legacy-trigger-service-jenkinsfile from old job trigger-service-jenkinsfile
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
