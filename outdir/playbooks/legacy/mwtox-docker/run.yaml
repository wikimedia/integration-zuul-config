- hosts: all
  name: Autoconverted job legacy-mwtox-docker from old job mwgate-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
