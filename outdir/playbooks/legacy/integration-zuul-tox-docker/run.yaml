- hosts: all
  name: Autoconverted job legacy-integration-zuul-tox-docker from old job integration-zuul-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
