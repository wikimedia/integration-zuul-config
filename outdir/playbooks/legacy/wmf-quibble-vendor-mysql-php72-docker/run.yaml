- hosts: all
  name: Autoconverted job legacy-wmf-quibble-vendor-mysql-php72-docker from old job
    wmf-quibble-vendor-mysql-php72-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
