- hosts: all
  name: Autoconverted job legacy-mediawiki-quibble-vendor-postgres-php72-docker from
    old job mediawiki-quibble-vendor-postgres-php72-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
