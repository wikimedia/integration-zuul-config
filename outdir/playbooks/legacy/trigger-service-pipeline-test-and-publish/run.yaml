- hosts: all
  name: Autoconverted job legacy-trigger-service-pipeline-test-and-publish from old
    job trigger-service-pipeline-test-and-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
