- hosts: all
  name: Autoconverted job legacy-analytics-wikistats from old job analytics-wikistats
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
