- hosts: all
  name: Autoconverted job legacy-pywikibot-core-tox-publish from old job pywikibot-core-tox-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
