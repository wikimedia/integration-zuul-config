- hosts: all
  name: Autoconverted job legacy-mediawiki-ruby-api-bundle-yard-publish from old job
    mediawiki-ruby-api-bundle-yard-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
