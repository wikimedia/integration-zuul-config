- hosts: all
  name: Autoconverted job legacy-oojs-ui-docker-publish from old job oojs-ui-docker-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
