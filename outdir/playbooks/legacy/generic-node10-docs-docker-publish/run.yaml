- hosts: all
  name: Autoconverted job legacy-generic-node10-docs-docker-publish from old job generic-node10-docs-docker-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
