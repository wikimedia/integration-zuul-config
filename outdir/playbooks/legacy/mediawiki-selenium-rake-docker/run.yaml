- hosts: all
  name: Autoconverted job legacy-mediawiki-selenium-rake-docker from old job mediawiki-selenium-rake-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
