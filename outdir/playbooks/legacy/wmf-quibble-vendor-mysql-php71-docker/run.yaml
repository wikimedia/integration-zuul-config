- hosts: all
  name: Autoconverted job legacy-wmf-quibble-vendor-mysql-php71-docker from old job
    wmf-quibble-vendor-mysql-php71-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
