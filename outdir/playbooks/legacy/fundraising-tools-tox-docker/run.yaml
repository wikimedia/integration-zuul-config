- hosts: all
  name: Autoconverted job legacy-fundraising-tools-tox-docker from old job fundraising-tools-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
