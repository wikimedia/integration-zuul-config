- hosts: all
  name: Autoconverted job legacy-wmf-quibble-core-vendor-mysql-php73-docker from old
    job wmf-quibble-core-vendor-mysql-php73-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
