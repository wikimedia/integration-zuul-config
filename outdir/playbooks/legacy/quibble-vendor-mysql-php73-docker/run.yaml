- hosts: all
  name: Autoconverted job legacy-quibble-vendor-mysql-php73-docker from old job quibble-vendor-mysql-php73-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
