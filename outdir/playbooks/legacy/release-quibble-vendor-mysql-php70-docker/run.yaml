- hosts: all
  name: Autoconverted job legacy-release-quibble-vendor-mysql-php70-docker from old
    job release-quibble-vendor-mysql-php70-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
