- hosts: all
  name: Autoconverted job legacy-wmf-utils-tox-docker from old job wmf-utils-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
