- hosts: all
  name: Autoconverted job legacy-composer-package-hhvm-docker from old job composer-package-hhvm-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
