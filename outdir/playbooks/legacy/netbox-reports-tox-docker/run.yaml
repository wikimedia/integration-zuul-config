- hosts: all
  name: Autoconverted job legacy-netbox-reports-tox-docker from old job netbox-reports-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
