- hosts: all
  name: Autoconverted job legacy-php-compile-hhvm-stretch-docker from old job php-compile-hhvm-stretch-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
