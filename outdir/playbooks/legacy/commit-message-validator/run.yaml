- hosts: all
  name: Autoconverted job legacy-commit-message-validator from old job commit-message-validator
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
