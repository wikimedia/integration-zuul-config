- hosts: all
  name: Autoconverted job legacy-acme-chief-tox-docker from old job acme-chief-tox-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
