- hosts: all
  name: Autoconverted job legacy-integration-config-zuul-layout-validate-docker from
    old job integration-config-zuul-layout-validate-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
