- hosts: all
  name: Autoconverted job legacy-operations-puppet-tests-stretch-docker from old job
    operations-puppet-tests-stretch-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
