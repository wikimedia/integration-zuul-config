- hosts: all
  name: Autoconverted job legacy-puppet-wmf-styleguide-rake-docker from old job puppet-wmf-styleguide-rake-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
