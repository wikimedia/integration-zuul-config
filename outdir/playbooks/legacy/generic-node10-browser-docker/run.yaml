- hosts: all
  name: Autoconverted job legacy-generic-node10-browser-docker from old job generic-node10-browser-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
