- hosts: all
  name: Autoconverted job legacy-trigger-blubber-pipeline-publish from old job trigger-blubber-pipeline-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
