- hosts: all
  name: Autoconverted job legacy-parsoidsvc-deploy-npm-node-6-docker from old job
    parsoidsvc-deploy-npm-node-6-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
