- hosts: all
  name: Autoconverted job legacy-mwcomposer-php70-docker from old job mwgate-composer-php70-docker
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
