- hosts: all
  name: Autoconverted job legacy-trigger-service-pipeline-test from old job trigger-service-pipeline-test
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
