- hosts: all
  name: Autoconverted job legacy-spicerack-tox-publish from old job spicerack-tox-publish
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '{{ ansible_user_dir }}/workspace'
        state: directory
