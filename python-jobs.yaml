- job-template:
    name: '{name}-pep8'
    defaults: use-zuul

    triggers:
     - zuul

    builders:
     - pep8

    publishers:
     - pep8

- job-template:
    name: '{name}-pyflakes'
    defaults: use-zuul

    triggers:
     - zuul

    builders:
     - pyflakes
