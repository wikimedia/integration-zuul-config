- job-template:
    name: '{name}-pep8'
    node: hasContintPackages
    defaults: use-zuul

    triggers:
     - zuul

    builders:
     - pep8

    publishers:
     - pep8

- job-template:
    name: '{name}-pyflakes'
    node: hasContintPackages
    defaults: use-zuul

    triggers:
     - zuul

    builders:
     - pyflakes
