- project:
    # mediawiki/services/cxserver.git
    name: 'cxserver'
    jobs:
     - '{name}-jslint'
     - '{name}-npm'

- project:
    # mediawiki/services/rcstream.git
    name: 'rcstream'
    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'
