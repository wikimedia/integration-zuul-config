# MediaWiki core has branches that contains lot of extensions. Whenever
# we test master (which has no submodule) and then a wmf branch (which
# have submodules) we end up refetching all the extensions which is really
# slow.
#
# This git macro hardcodes mediawiki/core to never update the submodules.
#
- scm:
    name: git-mwcore-nosubmodules
    scm:
     - git:
        url: '/srv/ssd/zuul/git/mediawiki/core'
        branches:
         - '$ZUUL_COMMIT'
        refspec: '$ZUUL_REF'
        disable-submodules: true
        # Avoid recloning large repositories, trust git-clean instead
        wipe-workspace: false
        clean: true

# FIXME: Both this one and the above don't do submodules, the only difference
# is the reference-repo. Merge these two?
- scm:
    name: git-mwcore
    scm:
     - git:
        url: 'git://zuul.eqiad.wmnet/mediawiki/core'
        reference-repo: '/srv/ssd/gerrit/mediawiki/core.git'
        branches:
         - '$ZUUL_COMMIT'
        refspec: '$ZUUL_REF'
        disable-submodules: true
        # mw/core is a huge repo so dont reclone it
        wipe-workspace: false
        clean: true

- scm:
    name: git-remote-zuul
    scm:
     - git:
        url: 'git://zuul.eqiad.wmnet/$ZUUL_PROJECT'
        reference-repo: '/srv/ssd/gerrit/$ZUUL_PROJECT.git'
        branches:
         - '$ZUUL_COMMIT'
        refspec: '$ZUUL_REF'

- scm:
    name: git-remote-zuul-no-submodules
    scm:
     - git:
        url: 'git://zuul.eqiad.wmnet/$ZUUL_PROJECT'
        reference-repo: '/srv/ssd/gerrit/$ZUUL_PROJECT.git'
        branches:
         - '$ZUUL_COMMIT'
        refspec: '$ZUUL_REF'
        disable-submodules: true
