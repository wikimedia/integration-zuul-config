- job-template:
    name: 'php-luasandbox-build'
    defaults: use-zuul
    triggers:
     - zuul

    builders:
     - php-extension

- project:
    name: 'php-luasandbox'
    gerrit-name: 'mediawiki/php/luasandbox'

    jobs:
     - php-luasandbox-build
