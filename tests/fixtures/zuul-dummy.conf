# Used by Jenkins job integration-zuul-layoutvalidation-gate

[zuul]

[connection gerrit]
driver = gerrit
server = dummy.example.org.
user = nonexistinguser

[connection gerrit_test]
driver = gerrit
server = dummy-test.example.org.
user = norealuser
