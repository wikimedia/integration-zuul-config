- project:
    name: 'mw-skins-erudite'

    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-mediawiki-strapping'

    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-webplatform'

    jobs:
     - '{name}-phplint'
