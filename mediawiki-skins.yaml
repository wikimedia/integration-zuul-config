- project:
    name: 'mw-skins-apex'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-BlueSky'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-BlueSpiceSkin'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-chameleon'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-CologneBlue'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Daddio'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Donate'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Dusk'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-erudite'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Example'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-GreyStuff'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Mask'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-mediawiki-strapping'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Modern'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-MonoBook'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Nimbus'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Nostalgia'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-p2wiki'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Schulenburg'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Splash'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Synagonism'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Tomas'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Truglass'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-Vector'
    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-skins-webplatform'
    jobs:
     - '{name}-phplint'
     - '{name}-jslint'
