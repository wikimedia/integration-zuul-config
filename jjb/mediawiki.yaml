# This file holds the configuration for all MediaWiki core related jobs.

# Note that mediawiki/core branches may have a lot of submodules, we thus
# usually need to NOT process submodules (eg: zuul-cloner).
# See https://phabricator.wikimedia.org/T44455

- publisher:
    name: compress-mw-debug-logs
    publishers:
        - postbuildscript:
            # MediaWiki debug logs are typically fairly large
            builders:
                - shell: 'find log/ -name "mw-debug-*.log" -exec gzip {} \+'
            # Always run it
            script-only-if-succeeded: false
            script-only-if-failed: false


- job:
    name: 'mediawiki-core-jsduck-docker-publish'
    node: DebianJessieDocker
    concurrent: false
    properties:
        - build-discarder:
            days-to-keep: 15
    triggers:
        - zuul
    builders:
        - docker-castor-load
        - docker-log-dir
        - docker-src-dir
        - docker-ci-src-setup-simple
        - docker-run-with-log-cache-src:
            image: docker-registry.wikimedia.org/releng/npm-test:0.6.1-s1
            args: 'doc'
            logdir: '/log'
        - doc-publish:
           docsrc: 'src/docs/js'
           docdest: 'mediawiki-core/$DOC_SUBPATH/js'
    publishers:
        - archive-log-allow-empty
        - castor-save-workspace-cache
        - docker-cleanup

- job:
    name: 'mediawiki-core-doxygen-docker'
    node: DebianJessieDocker
    # We dont want to overload CI when multiple branches have been updated
    concurrent: false
    triggers:
        - pollscm:
            cron: '@hourly'
    scm:
        - git:
            url: 'https://gerrit.wikimedia.org/r/p/mediawiki/core.git'
            do-not-fetch-tags: true
            branches:
                - :^origin/master
                - :^origin/REL\d+_\d+
            clean:
                before: true
                after: true
            basedir: src
            # Checkout to a local branch with 'origin/' stripped. That also set
            # GIT_LOCAL_BRANCH which we use later to publish the documentation.
            local-branch: '**'
    builders:
        - docker-log-dir
        - shell: |
            install -d -m 777 log/build/
        - shell: |
            rm -fR src/vendor && mkdir -p src/vendor
            git clone \
                --depth 1 \
                --reference /srv/git/mediawiki/vendor.git \
                --branch "$GIT_LOCAL_BRANCH" \
                -- \
                https://gerrit.wikimedia.org/r/p/mediawiki/vendor.git \
                src/vendor
        - shell: |
            touch src/LocalSettings.php
        - docker-run-with-log-cache-src:
            image: docker-registry.wikimedia.org/releng/doxygen:0.4.1
            options: '--workdir /src --entrypoint=/usr/bin/php'
            logdir: '/log'
            args: |
                maintenance/mwdocgen.php \
                    --no-extensions \
                    --output /log/build \
                    --version "$GIT_LOCAL_BRANCH" \
                    1> >(tee console.txt) \
                    2> >(tee errors.txt >&2)
            # IO redirections happen on the host.
            #
            # stderr is sent as stdin to a tee FIFO which write back to stderr
            # allowing to display and capture stderr.
        - shell: |
            # We want stdout/stderr published on doc.wikimedia.org
            install -m 666 console.txt log/build/
            install -m 666 errors.txt log/build/
        - doc-publish:
            docsrc: 'log/build/html'
            docdest: 'mediawiki-core/${GIT_LOCAL_BRANCH}/php'
        - docker-wipe-dir:
            dir: log/build
    publishers:
        - archive:
            artifacts: 'console.txt,errors.txt'
            allow-empty: true
        - beta-irc  # Spam #wikimedia-releng on failure
        - docker-cleanup

- builder:
    name: assert-no-mediawiki-errors
    builders:
        - shell:
            !include-raw:
                - assert-no-mediawiki-errors.bash

- project:
    name: mediawiki-core
    phpflavor:
        - hhvm
    jobs:
      - '{name}-jsduck-docker'

# Used to decouple MediaWiki related projects from the rest of the projects in
# gate-and-submit. See T107529.
- project:
    name: 'mwgate'
    jobs:
        - '{name}-composer-{phpflavor}-docker':
            phpflavor:
                - hhvm:
                    image: docker-registry.wikimedia.org/releng/composer-test-hhvm:0.2.6-s1
                - php70:
                    image: docker-registry.wikimedia.org/releng/composer-test:0.1.7-s1
                - php72:
                    image: docker-registry.wikimedia.org/releng/composer-test-php72:0.2.1-s1
        - '{name}-jsduck-docker'
        - '{name}-rake-docker'
        - '{name}-tox-docker'
        - '{name}-node10-docker'
        - '{name}-npm-browser-node-6-docker':
            docker_image_var: docker-registry.wikimedia.org/releng/npm-browser-test:0.4.1-s1

# Phan! (T132636)
- job:
    name: 'mediawiki-core-php72-phan-docker'
    node: DebianJessieDocker && m4executor
    concurrent: true
    properties:
     - build-discarder:
         days-to-keep: 15
    triggers:
     - zuul
    builders:
     - docker-log-dir
     - docker-src-dir
     - docker-cache-dir
     - docker-castor-load
     - docker-ci-src-setup-mw
     - docker-run-with-log-cache-src:
        options: '--volume "$(pwd)"/src:/mediawiki --entrypoint=/run-core.sh'
        image: 'docker-registry.wikimedia.org/releng/mediawiki-phan:0.3.1-s1'
        args: '-m checkstyle'
        logdir: '/log'
    publishers:
     - castor-save-workspace-cache
     - checkstyle:
        pattern: 'src/tests/phan/issues/latest'
        can-run-on-failed: true
        thresholds:
          failed:
            total-all: 1
     - docker-cleanup

- job:
    name: 'mediawiki-core-php72-phan-seccheck-docker'
    node: DebianJessieDocker && m4executor
    concurrent: true
    properties:
     - build-discarder:
         days-to-keep: 15
    triggers:
     - zuul
    builders:
     - docker-log-dir
     - docker-src-dir
     - docker-cache-dir
     - docker-ci-src-setup-mw
     - docker-run-with-log-cache-src:
         image: docker-registry.wikimedia.org/releng/mediawiki-phan-seccheck:0.4.0
         options: '--env THING_SUBNAME="" --env SECCHECK_MODE="mw-config.php" --volume "$(pwd)"/src:/mediawiki' # FIXME container should use /src
         args: '-m checkstyle'
         logdir: '/log'
    publishers:
     - checkstyle:
        pattern: 'src/seccheck-issues'
        can-run-on-failed: true
        thresholds:
          failed:
            total-all: 1
     - docker-cleanup

- job-template: &job_quibble
    name: quibble-{packages-source}-{database}-{php}-docker
    node: DebianJessieDocker && m4executor
    quibble_args: '--skip npm-test'
    concurrent: true
    triggers:
        - zuul
    properties:
        - build-discarder:
            days-to-keep: 7
    builders:
        - docker-castor-load
        - docker-log-dir
        - docker-run-with-log-and-workspace-cache:
            image: '{docker_image}'
            options: '--volume /srv/git:/srv/git:ro --tmpfs /workspace/db:size=320M'
            args: '--packages-source "{packages-source}" --db "{database}" --db-dir /workspace/db --git-parallel=8 {quibble_args}'
            logdir: '/workspace/log'
    publishers:
        - junit:
            allow-empty-results: true
            results: 'log/junit*.xml'
        - compress-mw-debug-logs
        - archive-log-allow-empty
        - castor-save-workspace-cache
        # Job duration to statsd - T201972
        - conditional-publisher:
            - condition-kind: shell
              condition-command: '
                [ $JOB_NAME = "mediawiki-quibble-vendor-mysql-hhvm-docker" ] &&
                [ $ZUUL_PROJECT = "mediawiki/core" ] &&
                [ $ZUUL_BRANCH = "master" ]'
              action:
                - record-node-stats
        - docker-cleanup
    wrappers:
        - timeout:
            timeout: 60 # HHVM quibble jobs for Wikibase 'stack' take ~45 mins
            fail: true
        - timestamps
        - ansicolor

# Skipping selenium tests - T196960
- job-template:
    !!merge : *job_quibble
    name: quibble-{packages-source}-{database}-{php}-noselenium-docker
    quibble_args: '--skip selenium npm-test'
    triggers:
        - zuul

# For the master and wmf/* branches
- job-template:
    !!merge : *job_quibble
    name: wmf-quibble-vendor-mysql-{php}-docker
    # We do not run mediawiki/core tests with extensions installed
    # https://phabricator.wikimedia.org/T197469#4293142
    quibble_args: '--phpunit-testsuite=extensions --skip npm-test'
    triggers:
        - zuul

# HACK: Don't run composer test for mediawiki/core, it's
# already run in a separate job
- job-template:
    !!merge : *job_quibble
    name: wmf-quibble-core-vendor-mysql-{php}-docker
    # We do not run mediawiki/core tests with extensions installed
    # https://phabricator.wikimedia.org/T197469#4293142
    quibble_args: '--phpunit-testsuite=extensions --skip composer-test npm-test'
    triggers:
        - zuul


- job-template:
    !!merge : *job_quibble
    name: mediawiki-quibble-{packages-source}-{database}-{php}-docker
    quibble_args: '--skip composer-test npm-test'
    triggers:
        - zuul

- job-template:
    !!merge : *job_quibble
    name: 'mediawiki-quibble-api-testing-vendor-{database}-{php}-docker'
    quibble_args: '-c "mediawiki/tools/api-testing/ci.sh" mediawiki/tools/api-testing'
    triggers:
        - zuul

- project:
    name: mediawiki-api-testing
    jobs:
        - mediawiki-quibble-api-testing-vendor-{database}-{php}-docker:
            php:
                - php72:
                    docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-php72:0.0.34-1
            packages-source: vendor
            database:
                - mysql
                - sqlite

- job-template:
    name: mediawiki-quibble-composertest-{php}-docker
    node: DebianJessieDocker && m4executor
    concurrent: true
    triggers:
        - zuul
    builders:
        - docker-castor-load
        - docker-log-dir
        - docker-run-with-log-and-workspace-cache:
            image: '{docker_image}'
            options: '--volume /srv/git:/srv/git:ro'
            args: '--packages-source "vendor" --git-parallel=8 --skip-install --run composer-test'
            logdir: '/workspace/log'
    publishers:
        - archive-log-allow-empty
        - castor-save-workspace-cache
        - docker-cleanup
    wrappers:
        - timeout:
            timeout: 45  # npm is broken T198348
            fail: true
        - timestamps
        - ansicolor

# Per-patch coverage for MediaWiki core.
- job-template:
    !!merge : *job_quibble
    name: 'mwcore-phpunit-coverage-patch'
    quibble_args: '--run=phpunit'
    triggers:
        - zuul
    builders:
        - docker-castor-load
        - docker-log-dir
        - docker-run-with-log-and-workspace-cache:
            image: '{docker_image}'
            options: '--volume /srv/git:/srv/git:ro --tmpfs /workspace/db:size=320M'
            args: '--packages-source {packages-source} --db {database} --db-dir /workspace/db --commands "phpunit-patch-coverage check --command \"php -d zend_extension=xdebug.so tests/phpunit/phpunit.php\" --html /workspace/log/coverage.html"'
            logdir: '/workspace/log'
    publishers:
     - archive:
         artifacts: 'log/coverage.html'
         allow-empty: true
     - docker-cleanup
    wrappers:
     - timeout:
         timeout: 60
         fail: true
     - ansicolor
     - timestamps

# Per-patch performance report for MediaWiki core.
- job:
    !!merge : *job_quibble
    name: 'mediawiki-fresnel-patch-docker'
    concurrent: false
    triggers:
        - zuul
    builders:
        - docker-log-dir
        - docker-run-with-log:
            image: 'docker-registry.wikimedia.org/releng/quibble-fresnel:0.0.34-2'
            options: '--volume /srv/git:/srv/git:ro --tmpfs /workspace/db:size=320M'
            args: '--packages-source vendor --db mysql --db-dir /workspace/db --skip-deps --commands "mediawiki-fresnel-patch"'
            logdir: '/workspace/log'
    publishers:
        - compress-mw-debug-logs
        - archive-log-allow-empty
        - docker-cleanup
    wrappers:
     - timeout:
         timeout: 30
         fail: true
     - ansicolor
     - timestamps

- job-template:
    !!merge : *job_quibble
    name: 'mwext-phpunit-coverage-patch-docker'
    quibble_args: '--commands=mwext-phpunit-coverage-patch'
    triggers:
        - zuul

# XXX copy pasted to pass --volume cover:/workspace/cover
- job-template:
    !!merge : *job_quibble
    name: 'mwext-phpunit-coverage-docker-publish'
    quibble_args: '--commands=mwext-phpunit-coverage'
    triggers:
        - zuul
    builders:
        - docker-castor-load
        - docker-wipe-dir:
            dir: 'cover'
        - docker-log-dir
        - docker-run-with-log-and-workspace-cache:
            image: '{docker_image}'
            options: '--volume /srv/git:/srv/git:ro --volume "$(pwd)"/cover:/workspace/cover --tmpfs /workspace/db:size=320M'
            args: '--packages-source "{packages-source}" --db "{database}" --db-dir /workspace/db {quibble_args}'
            logdir: '/workspace/log'
    publishers:
        - archive-log-allow-empty
        - castor-save-workspace-cache
        - postbuildscript:
            builders:
                - cover-extensions-publish:
                    src: 'cover'
                    # Should be equivalent to $EXT_NAME
                    dest: '$DOC_BASENAME'
                - docker-wipe-dir:
                    dir: 'cover'
        - docker-cleanup

- project:
    name: mwcore-codehealth
    packages-source: composer
    quibble_args: '--skip-install --commands=mediawiki-coverage'
    database: mysql
    jobs:
      - mwcore-codehealth-{name}:
         # Used for analyzing gerrit patch sets.
         name: 'patch'
         branch: '$ZUUL_CHANGE'
         concurrent: true
         sonar_args: |
             -Dsonar.projectKey=mediawiki-core \
             -Dsonar.projectName=mediawiki-core \
             -Dsonar.organization=wmftest \
             -Dsonar.host.url=https://sonarcloud.io \
             -Dsonar.branch.target="$ZUUL_BRANCH" \
             -Dsonar.branch.name={branch} \
             -X
      - mwcore-codehealth-{name}:
         # Used for analyzing the master branch on postmerge.
         name: 'master-non-voting'
         branch: '$ZUUL_BRANCH'
         concurrent: false
         sonar_args: |
             -Dsonar.projectKey=mediawiki-core \
             -Dsonar.projectName=mediawiki-core \
             -Dsonar.organization=wmftest \
             -Dsonar.host.url=https://sonarcloud.io \
             -X

- job-template:
    name: 'mwcore-codehealth-{name}'
    node: DebianJessieDocker && m4executor
    concurrent: '{concurrent}'
    branch: '$ZUUL_BRANCH'
    properties:
     - build-discarder:
         days-to-keep: 15
    triggers:
     - zuul

    wrappers:
     - ansicolor
     - timeout:
         timeout: 30
         fail: true
     - timestamps
     - credentials-binding:
           - text:
               credential-id: SONAR_API_KEY
               variable: SONAR_API_KEY
    builders:
    - docker-log-dir
    - docker-src-dir
    - docker-castor-load
    - docker-run-with-log-and-workspace-cache:
        image: docker-registry.wikimedia.org/releng/quibble-coverage:0.0.34-5
        options: '-e CODEHEALTH=1 --volume "$(pwd)"/src:/workspace/src --volume "$(pwd)"/cover:/workspace/cover --volume /srv/git:/srv/git:ro --tmpfs /workspace/db:size=320M'
        args: '--packages-source "{packages-source}" --git-parallel=8 {quibble_args}'
        logdir: '/workspace/log'
    - docker-run-with-log-and-workspace-cache:
        image: 'docker-registry.wikimedia.org/releng/java8-sonar-scanner:0.4.12'
        options: '--volume "$(pwd)"/src:/workspace/src'
        logdir: '/workspace/log'
        args: '{sonar_args}'
    publishers:
      - docker-cleanup

- project:
    name: mwext-codehealth
    packages-source: composer
    database: mysql
    jobs:
      - mwext-codehealth-{name}:
         # Used for analyzing gerrit patch sets.
         name: 'patch'
         concurrent: true
         branch: '$ZUUL_CHANGE'
         sonar_args: |
             -Dsonar.projectKey=${{ZUUL_PROJECT//\//-}} \
             -Dsonar.projectName=${{ZUUL_PROJECT//\//-}} \
             -Dsonar.organization=wmftest \
             -Dsonar.host.url=https://sonarcloud.io \
             -Dsonar.branch.target="$ZUUL_BRANCH" \
             -Dsonar.branch.name={branch} \
             -X
         publishers:
           - docker-cleanup
      - mwext-codehealth-{name}:
         # Used for analyzing the master branch on postmerge.
         name: 'master-non-voting'
         concurrent: false
         branch: '$ZUUL_BRANCH'
         sonar_args: |
             -Dsonar.projectKey=${{ZUUL_PROJECT//\//-}} \
             -Dsonar.projectName=${{ZUUL_PROJECT//\//-}} \
             -Dsonar.organization=wmftest \
             -Dsonar.host.url=https://sonarcloud.io \
             -X
         publishers:
             - archive-log-allow-empty
             - castor-save-workspace-cache
             - postbuildscript:
                   script-only-if-succeeded: false
                   script-only-if-failed: false
                   builders:
                       -   cover-extensions-publish:
                               src: 'cover'
                               # Should be equivalent to $EXT_NAME
                               dest: '$DOC_BASENAME'
                       -   docker-wipe-dir:
                               dir: 'cover'
             - docker-cleanup

- job-template:
    name: 'mwext-codehealth-{name}'
    node: DebianJessieDocker && m4executor
    concurrent: '{concurrent}'
    branch: '$ZUUL_BRANCH'
    properties:
     - build-discarder:
         days-to-keep: 15
    triggers:
     - zuul

    wrappers:
     - ansicolor
     - timeout:
         timeout: 30
         fail: true
     - timestamps
     # SONAR_API_KEY is in Jenkins credentials store
     # https://integration.wikimedia.org/ci/credentials/
     - credentials-binding:
           - text:
               credential-id: SONAR_API_KEY
               variable: SONAR_API_KEY

    builders:
    - docker-log-dir
    - docker-src-dir
    - docker-castor-load
    - docker-wipe-dir:
        dir: 'cover'
    - docker-run-with-log-and-workspace-cache:
        image: docker-registry.wikimedia.org/releng/quibble-coverage:0.0.34-5
        options: '-e CODEHEALTH=1 --volume "$(pwd)"/src:/workspace/src --volume "$(pwd)"/cover:/workspace/cover --volume /srv/git:/srv/git:ro'
        args: '--packages-source "{packages-source}" --git-parallel=8 --skip-install --commands=mwext-phpunit-coverage'
        logdir: '/workspace/log'
    - docker-run-with-log-and-workspace-cache:
        image: docker-registry.wikimedia.org/releng/node10-test:0.5.0
        options: '--volume "$(pwd)"/src/extensions/"$EXT_NAME":/src'
        logdir: '/workspace/log'
        args: '--if-present test:unit'
    - shell: |
        if [ -f src/extensions/"$EXT_NAME"/coverage/lcov.info ]; then
          sed -ie "s#/src/#/workspace/src/extensions/$EXT_NAME/#g" src/extensions/"$EXT_NAME"/coverage/lcov.info
        fi
    - docker-run-with-log-and-workspace-cache:
        image: 'docker-registry.wikimedia.org/releng/java8-sonar-scanner:0.4.12'
        options: '--volume "$(pwd)"/src:/workspace/src'
        logdir: '/workspace/log'
        args: '{sonar_args}'
    publishers: '{publishers}'

# XXX copy pasted to pass --volume cover:/workspace/cover and run cover-publish
- job-template:
    !!merge : *job_quibble
    name: 'mwcore-phpunit-coverage-master'
    concurrent: false
    properties:
        - build-discarder:
            days-to-keep: 30
    quibble_args: '--commands=mediawiki-coverage'
    triggers:
     - timed: '0 3,15 * * *'
    wrappers:
     - timeout:
         timeout: 300
         fail: true
     - ansicolor
     - timestamps
    builders:
        - docker-wipe-dir:
            dir: 'cache'
        - docker-wipe-dir:
            dir: 'cover'
        - docker-log-dir
        - docker-run-with-log-and-workspace-cache:
            image: '{docker_image}'
            options: '--volume /srv/git:/srv/git:ro --volume "$(pwd)"/cover:/workspace/cover --tmpfs /workspace/db:size=320M'
            args: '--packages-source "{packages-source}" --db "{database}" --db-dir /workspace/db {quibble_args}'
            logdir: '/workspace/log'
        - cover-publish:
            src: 'cover'
            dest: 'mediawiki-core'
    publishers:
        - archive-log-allow-empty
        - cloverphp:
            xml-location: 'log/clover.xml'
        - beta-irc  # Spam #wikimedia-releng on failure
        - docker-cleanup

- job:
    name: 'mediawiki-core-phpmetrics-docker'
    description: |
        <p>Generate <a href="https://doc.wikimedia.org/mediawiki-core/master/phpmetrics/">
        https://doc.wikimedia.org/mediawiki-core/master/phpmetrics/</a>.</p>
        <p>Job is managed by <a href="https://www.mediawiki.org/wiki/CI/JJB">Jenkins Job Builder</a>.</p>
    node: DebianJessieDocker
    concurrent: false
    triggers:
        - pollscm:
            cron: '@daily'
    scm:
        - git:
            url: 'https://gerrit.wikimedia.org/r/p/mediawiki/core.git'
            shallow-clone: true
            branches:
                - master
            clean:
                before: true
                after: true
            basedir: src
            # Checkout to a local branch with 'origin/' stripped. That also set
            # GIT_LOCAL_BRANCH which we use later to publish the documentation.
            local-branch: '**'
    wrappers:
        - timeout:
            timeout: 30
            fail: true
        - ansicolor
        - timestamps
    builders:
        - docker-log-dir
        - docker-wipe-dir:
            dir: 'phpmetrics-report'
        - docker-run:
            options: '-v "$(pwd)"/phpmetrics-report:/phpmetrics-report -v "$(pwd)"/log:/log -v "$(pwd)"/src:/src'
            image: docker-registry.wikimedia.org/releng/phpmetrics:2.4.3-s1
            args: '--report-html=/phpmetrics-report --report-violations=/log/violations-phpmetrics.xml .'
        - doc-publish:
            docsrc: phpmetrics-report
            docdest: mediawiki-core/master/phpmetrics
    publishers:
        - archive-log-allow-empty
        - beta-irc # Spam #wikimedia-releng on failure
        - email-ext:
            recipients: hashar@free.fr qa-alerts@lists.wikimedia.org
            content-type: html
            failure: true
            unstable: true
        - docker-cleanup

- job-template:
    !!merge : *job_quibble
    name: 'mwselenium-quibble-docker'
    quibble_args: '--skip-deps --commands=mwselenium'
    triggers:
        - zuul

- job:
    name: 'quibble-integration'
    project-type: matrix
    execution-strategy:
        sequential: true
    parameters:
        - matrix-combinations:
            name: combo
            description: 'Select matrix combinations'
    axes:
        - axis:
            type: label-expression
            name: label
            values:
                - contint1001
        - axis:
            name: ZUUL_BRANCH
            type: user-defined
            values: &mediawiki_supported_branches
                - master
                - REL1_33
                - REL1_32
                - REL1_31
        - axis:
            name: packagessource
            type: user-defined
            values:
                - composer
                - vendor
        - axis:
            name: database
            type: user-defined
            values:
                - mysql
                - postgres
                - sqlite
        - axis:
            name: php
            type: user-defined
            values:
                - php70
                - php71
                - php72
                - php73
                - hhvm
    builders:
        - trigger-builds:
            - project: 'mediawiki-quibble-${packagessource}-${database}-${php}-docker'
              block: true
              predefined-parameters: |
                    projectName=mediawiki-quibble-${packagessource}-${database}-${php}-docker
                    ZUUL_URL=https://gerrit.wikimedia.org/r/p
                    ZUUL_PROJECT=mediawiki/core
                    ZUUL_BRANCH=$ZUUL_BRANCH
                    ZUUL_REF=$ZUUL_BRANCH

- project:
    name: quibble-jobs
    packages-source:
        - composer
        - vendor
    database:
        - mysql
        - postgres
        - sqlite
    php:
        - php70:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-php70:0.0.34-1
        - php71:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-php71:0.0.34-1
        - php72:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-php72:0.0.34-1
        - php73:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-php73:0.0.34-1
        - hhvm:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-hhvm:0.0.34-1
    jobs:
        - quibble-{packages-source}-{database}-{php}-docker
        - quibble-{packages-source}-{database}-{php}-noselenium-docker
        - mediawiki-quibble-{packages-source}-{database}-{php}-docker
        - mediawiki-quibble-composertest-{php}-docker
        - mwcore-phpunit-coverage-patch:
            docker_image: docker-registry.wikimedia.org/releng/quibble-coverage:0.0.34
            packages-source: vendor
            database: sqlite
        - mwcore-phpunit-coverage-master:
            docker_image: docker-registry.wikimedia.org/releng/quibble-coverage:0.0.34
            packages-source: vendor
            database: sqlite
        - mwext-phpunit-coverage-patch-docker:
            docker_image: docker-registry.wikimedia.org/releng/quibble-coverage:0.0.34
            packages-source:  composer
            database: mysql
        - mwext-phpunit-coverage-docker-publish:
            docker_image: docker-registry.wikimedia.org/releng/quibble-coverage:0.0.34
            packages-source:  composer
            database: mysql
        - mwselenium-quibble-docker:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-bundle:0.0.34-1
            packages-source: vendor
            database: mysql

# MediaWiki jobs for deployment to the Wikimedia cluster
- project:
    name: quibble-jobs-for-wikimedia

    # We never use composer
    packages-source: vendor
    # We only use MariaDB/MySQL
    database: mysql
    # And only HHVM / php7.2
    php:
        - hhvm:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-hhvm:0.0.34-1
        - php72:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-php72:0.0.34-1
    jobs:
        - wmf-quibble-vendor-mysql-{php}-docker
        - wmf-quibble-core-vendor-mysql-{php}-docker

# General MW-Selenium job used to dogfood the builder against MW core.
- job-template:
    !!merge : *job_quibble
    name: 'mediawiki-selenium-integration-docker'
    triggers:
        - zuul

- project:
    name: mediawiki-selenium-integration
    jobs:
        - mediawiki-selenium-integration-docker:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-bundle:0.0.34-1
            packages-source: vendor
            database: mysql
            quibble_args: '--skip-deps --commands=mwselenium -- mediawiki/selenium'

- job-template:
    !!merge : *job_quibble
    name: parsoidsvc-parsertests-docker
    node: DebianJessieDocker
    concurrent: true
    wrappers:
        - timeout:
            timeout: 10  # minutes
            fail: true
        - timestamps
        - ansicolor
    triggers:
        - zuul

- project:
    name: parsoidsvc-parsertests
    jobs:
        - parsoidsvc-parsertests-docker:
            docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-hhvm:0.0.34-1
            packages-source: composer
            database: sqlite
            quibble_args: '--commands=mediawiki/services/parsoid/tools/ci-mw-parsertests.sh'

- job-template:
    !!merge : *job_quibble
    parameters:
        # Hint about the job requiring dependencies. They MUST be set by Zuul.
        - string:
            name: 'EXT_DEPENDENCIES'
            description: 'Extensions to add, separated by "\n". Set by Zuul.'
    name: wikibase-{kind}-docker
    triggers:
        - zuul

- project:
    name: wikibase
    docker_image: docker-registry.wikimedia.org/releng/quibble-stretch-php72:0.0.34-1
    packages-source: composer
    kind:
        - client:
            database: mysql
            phpunit-params: '--group Wikibase,WikibaseClient'
        - repo:
            database: sqlite
            phpunit-params: '--group Wikibase,WikibaseAPI,Purtle'
    jobs:
        - wikibase-{kind}-docker:
            quibble_args: >
                --commands 'extensions/Wikibase/build/jenkins/mw-apply-wb-settings.sh -r {kind}'
                'cp --verbose $WORKSPACE/src/LocalSettings.php $LOG_DIR'
                'php maintenance/update.php --quick'
                'php tests/phpunit/phpunit.php --verbose --log-junit "$LOG_DIR/junit-wikibase-{kind}.xml" {phpunit-params}'

- project:
    name: mediawiki-wdio-selenium
    project: MediaWiki
    recipients: betacluster-alerts@lists.wikimedia.org cparle@wikimedia.org qa-alerts@lists.wikimedia.org zfilipin@wikimedia.org
    repository: mediawiki/core
    jobs:
        - 'selenium-daily-{sitename}-{project}':
            sitename:
                - beta:
                    mw_server: 'https://en.wikipedia.beta.wmflabs.org'
                - betacommons:
                    mw_server: 'https://commons.wikimedia.beta.wmflabs.org'

- project:
    name: 'mediawiki-php-security'
    repogroup: 'mediawiki'
    host: 'https://gerrit.wikimedia.org/r/p'
    projects:
      - mediawiki/core
      - mediawiki/vendor
    branches: *mediawiki_supported_branches
    jobs:
     - '{repogroup}-php-security'
