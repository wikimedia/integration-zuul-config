- project:
    name: blubber
    pipeline:
      - test
      - rehearse
      - publish
    jobs:
      - 'trigger-{name}-pipeline-{pipeline}'
      - '{name}-pipeline-{pipeline}'

- project:
    name: mediawiki
    pipeline:
      - dev
    jobs:
      - 'trigger-{name}-pipeline-{pipeline}'
      - '{name}-pipeline-{pipeline}'
