- project:
    name: search
    javadocgoals: clean javadoc:javadoc
    project:
        - extra
        - highlighter
        - ltr
        - repository-swift
    jobs:
        - '{name}-{project}-maven'
        - '{name}-{project}-maven-site-publish'

- project:
    name: search-xgboost
    jobs:
        - '{name}-maven':
            root_pom_var: jvm-packages/pom.xml
