# CI for gerrit
- job:
    name: 'gerrit-docker'
    node: Docker
    wrappers:
      # needs to be 1 hour and 30 mins to give gerrit enough time to build.
      # It can sometimes take a while.
      - timeout:
          timeout: 90
          fail: true
      - timestamps
      - credentials-binding:
          - text:
              credential-id: composer-github-oauthtoken
              variable: COMPOSER_GITHUB_OAUTHTOKEN
    triggers:
     - zuul
    builders:
      - docker-castor-load
      - docker-src-dir
      - docker-log-dir
      # .gitsubmodules uses relative url, we need to point to upstream gerrit
      # repository to fetch them.
      - docker-ci-src-setup:
          submodules: false
      - docker-run-with-log-cache-src:
          image: docker-registry.wikimedia.org/releng/ci-src-setup-simple:0.4.2-s1
          options: --entrypoint=git
          args: >
              -c remote.origin.url=https://gerrit.googlesource.com/gerrit
              submodule update --init --recursive
      - docker-run-with-log-cache-src:
          image: 'docker-registry.wikimedia.org/releng/gerrit:1.0.0'
          options: '--volume /srv/git:/srv/git:ro'
          # Build various `.war` targets:
          # - gerrit: only core
          # - withdocs: core + doc
          # - release: core + doc + plugins
          args: gerrit withdocs release
    publishers:
      - archive:
         artifacts: >
           log/execroot/*/bazel-out/k8-fastbuild/bin/*.war,
           log/execroot/*/bazel-out/k8-fastbuild/bin/plugins/core.zip,
      - castor-save-workspace-cache
      - docker-cleanup

- job-template: &job_template_maven-java8-docker
    name: '{name}-maven-java8-docker'
    node: Docker
    # Projects are by default built on Java 8. To build on Java 11, use the
    # {name}-maven-java11-docker job template, which will replace this docker
    # image with the java 11 version.
    docker_image_var: docker-registry.wikimedia.org/releng/maven-java8:1.0.0
    triggers:
        - zuul
    # The project is expected to provide a maven wrapper at the root of the
    # project. If another entry point is required for a project (for example
    # to setup some build prerequisites) 'maven_path' variables can be
    # overridden. Note that the sonar analysis step will always use the
    # maven wrapper provided by the project.
    #   maven_path: /some/entry/point
    maven_path: '/src/mvnw'

    maven_args: 'clean install'
    # The pom.xml is expected to be at the root of the project. If it is
    # somewhere else, the path can be specified here, relative to "workdir"
    # (see below).
    pom: 'pom.xml'
    # The working directory. It should usually be the root of the project.
    # Maven should be able to run from anywhere, but in some cases a project
    # configuration might depend on a specific working directory. Ideally the
    # Maven configuration should be fixed to be workdir independent, but if
    # that's not practical, the working directory can be overridden. Note that
    # in that case, it might be necessary to adapt the "pom" argument as well.
    workdir: '/src'
    builders:
        - docker-castor-load
        - docker-log-dir
        - docker-src-dir
        - docker-ci-src-setup-simple
        - docker-run-with-log-cache-src:
            # run Maven Wrapper directly from the project
            options: '--entrypoint={maven_path} --workdir={workdir}'
            image: '{obj:docker_image_var}'
            args: '-gs /settings.xml --batch-mode --file {pom} {maven_args}'

        # The branch holding the patch is named $ZUUL_BRANCH
        #
        # For patch analysis, we need to match sonar.branch.name parameter
        # which is set to {branch}which is the change number + the patchset.
        - docker-run-with-log-cache-src:
              image: '{obj:docker_image_var}'
              options: '--entrypoint=git'
              args: 'branch -m "$ZUUL_CHANGE-$ZUUL_PATCHSET"'
        # The comparison is done against sonar.branch.target which is
        # $ZUUL_BRANCH, make it point to the previous patchset.
        - docker-run-with-log-cache-src:
              image: '{obj:docker_image_var}'
              options: '--entrypoint=git'
              args: 'checkout -B $ZUUL_BRANCH FETCH_HEAD^'
        # Set working copy back to branch holding the patch
        - docker-run-with-log-cache-src:
              image: '{obj:docker_image_var}'
              options: '--entrypoint=git'
              args: 'checkout "$ZUUL_CHANGE-$ZUUL_PATCHSET"'

        - docker-run-with-log-cache-src:
            # This runs a sonar analysis after the main build. It relies on
            # Maven `install` target having been reached in the previous step.
            # It requires at least the following configurations in the pom.xml
            # of the project:
            # * sonar plugin declared and configured
            # * Sonar API key loaded via the environment variable `SONAR_API_KEY`
            #
            # minimal example:
            #   <properties>
            #     <sonar.host.url>https://sonarcloud.io</sonar.host.url>
            #     <sonar.login>${env.SONAR_API_KEY}</sonar.login>
            #     <sonar.organization>wmftest</sonar.organization>
            #   </properties>
            #   <build>
            #     <pluginManagement>
            #       <plugins>
            #         <plugin>
            #           <groupId>org.sonarsource.scanner.maven</groupId>
            #           <artifactId>sonar-maven-plugin</artifactId>
            #           <version>3.8.0.2131</version>
            #         </plugin>
            #       </plugins>
            #     </pluginManagement>
            #   </build>
            #
            # Note that Sonar must be running on Java 11, but can analyze code
            # built on Java 8.
            options: '--entrypoint=/src/mvnw --workdir={workdir}'
            image: 'docker-registry.wikimedia.org/releng/maven-java11:1.0.0'
            args: >
                -gs /settings.xml --batch-mode --file {pom} -X
                sonar:sonar
                -Dsonar.verbose=true
                -Dsonar.analysis.allowCommentOnMaster="1"
                -Dsonar.analysis.gerritProjectName="$ZUUL_PROJECT"
                -Dsonar.branch.target="$ZUUL_BRANCH"
                -Dsonar.branch.name="$ZUUL_CHANGE-$ZUUL_PATCHSET"
    publishers:
        - archive-log-allow-empty
        - castor-save-workspace-cache
        - docker-cleanup
    wrappers:
      - timeout:
          timeout: '{timeout|30}'
          fail: true
      - timestamps
      - credentials-binding:
          - text:
              credential-id: composer-github-oauthtoken
              variable: COMPOSER_GITHUB_OAUTHTOKEN
          - text:
                # SONAR_API_KEY is in Jenkins credentials store
                # https://integration.wikimedia.org/ci/credentials/
                credential-id: SONAR_API_KEY
                variable: SONAR_API_KEY

# Convenience alias with name/project variables
- job-template:
    !!merge : *job_template_maven-java8-docker
    name: '{name}-{project}-maven-java8-docker'
    # Reinject Zuul parameters since JJB strip for some reason
    triggers:
     - zuul

- job-template: &job_template_maven-java11-docker
    !!merge : *job_template_maven-java8-docker
    name: '{name}-maven-java11-docker'
    docker_image_var: docker-registry.wikimedia.org/releng/maven-java11:1.0.0
    # Reinject Zuul parameters since JJB strip for some reason
    triggers:
     - zuul

- job-template:
    !!merge : *job_template_maven-java11-docker
    name: '{name}-{project}-maven-java11-docker'
    # Reinject Zuul parameters since JJB strip for some reason
    triggers:
     - zuul

# Generate project documentation using docker and publish it
- job-template:
    name: '{name}-docker-doc-publish'
    node: Docker
    concurrent: false
    triggers:
     - zuul
    builders:
     - docker-cache-dir
     - docker-log-dir
     - docker-src-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
         image: '{image}'
         args: '{obj:args|}'
         options: >-
           --workdir /src
     - doc-publish:
        docsrc: '{docsrc}'
        docdest: '{docdest}'
    publishers:
     - archive-log-allow-empty
     - docker-cleanup

# Same as '{name}-maven-java8-docker' but src is bindmounted so we can later
# retrieve the site from /src/target/ and push it to doc.wikimedia.org. This
# job template is meant to be run as a post merge step.
- job-template: &job_template_maven-java8-docker-site-publish
    name: '{name}-maven-java8-docker-site-publish'
    node: Docker
    concurrent: false
    docker_image_var: docker-registry.wikimedia.org/releng/maven-java8:1.0.0
    # The project is expected to provide a maven wrapper at the root of the
    # project. If another entry point is required for a project (for example
    # to setup some build prerequisites) 'maven_path' variables can be
    # overridden. Note that the sonar analysis step will always use the
    # maven wrapper provided by the project.
    #   maven_path: /some/entry/point
    maven_path: '/src/mvnw'

    maven_args: 'clean install'
    # The pom.xml is expected to be at the root of the project. If it is
    # somewhere else, the path can be specified here, relative to "workdir"
    # (see below).
    pom: 'pom.xml'
    # The working directory. It should usually be the root of the project.
    # Maven should be able to run from anywhere, but in some cases a project
    # configuration might depend on a specific working directory. Ideally the
    # Maven configuration should be fixed to be workdir independent, but if
    # that's not practical, the working directory can be overridden. Note that
    # in that case, it might be necessary to adapt the "pom" argument as well.
    workdir: '/src'
    # The Maven site is generated in this directory. For projects that don't
    # follow a standard structure, this can be overridden.
    site_dir: 'src/target/staging'
    triggers:
        - zuul
    builders:
        - docker-castor-load
        - docker-log-dir
        - docker-src-dir
        - docker-ci-src-setup-simple
        # Build the maven project, runs the validation. The artifacts are
        # reused for the later steps (site and sonar analysis).
        - docker-run-with-log-cache-src:
            image: '{obj:docker_image_var}'
            options: '--entrypoint={maven_path} --workdir={workdir}'
            args: '-gs /settings.xml --batch-mode --file {pom} {maven_args}'
        # Sonar analysis (see '{name}-maven-java8-docker' for details)
        - docker-run-with-log-cache-src:
            image: 'docker-registry.wikimedia.org/releng/maven-java11:1.0.0'
            options: '--entrypoint=/src/mvnw --workdir={workdir}'
            args: >
                -gs /settings.xml --batch-mode --file {pom}
                sonar:sonar
                -Dsonar.analysis.allowCommentOnMaster="1"
                -Dsonar.analysis.gerritProjectName="$ZUUL_PROJECT"
                -Dsonar.branch.name="$ZUUL_BRANCH"
        # Generate Maven site, which will be published by the 'doc-publish'
        # step below.
        - docker-run-with-log-cache-src:
            image: '{obj:docker_image_var}'
            options: '--entrypoint={maven_path} --workdir={workdir}'
            args: '-gs /settings.xml --batch-mode --file {pom} site site:stage'
    wrappers:
      - timeout:
          timeout: '{timeout|30}'
          fail: true
      - timestamps
      - credentials-binding:
          # SONAR_API_KEY is in Jenkins credentials store
          # https://integration.wikimedia.org/ci/credentials/
          - text:
              credential-id: SONAR_API_KEY
              variable: SONAR_API_KEY
          - text:
              credential-id: composer-github-oauthtoken
              variable: COMPOSER_GITHUB_OAUTHTOKEN
    publishers:
        - postbuildscript:
              builders:
                - build-steps:
                    - doc-publish:
                        docsrc: '{site_dir}'
                        docdest: '{name}'
        - archive-log-allow-empty
        - castor-save-workspace-cache
        - docker-cleanup

- job-template:
    name: '{name}-{project}-maven-java8-docker-site-publish'
    !!merge : *job_template_maven-java8-docker-site-publish
    triggers:
        - zuul
    publishers:
        - postbuildscript:
              builders:
                - build-steps:
                    - doc-publish:
                        docsrc: 'src/target/staging'
                        docdest: '{name}-{project}'
        - archive-log-allow-empty
        - castor-save-workspace-cache
        - docker-cleanup

- job-template: &job_template_maven-release-docker
    name:       '{name}-maven-release-docker'
    node:       Docker
    concurrent: false
    parameters:
        - string:
            name:        'ZUUL_URL'
            default:     'https://maven-release-user@gerrit.wikimedia.org/r'
            description: >
                         Base URL to clone the repository from.
                         HTTPS credentials for Gerrit are injected by Jenkins'
        - string:
            name:        'ZUUL_PROJECT'
            default:     '{release-project}'
            description: 'The Gerrit project to clone'
        - string:
            name:        'ZUUL_REF'
            default:     '{release-branch}'
            description: 'A git reference to fetch. Used by "git fetch"'
        - string:
            name:        'ZUUL_BRANCH'
            default:     '{release-branch}'
            description: 'Name of the local branch to checkout to'
    wrappers:
        # Credentials for Gerrit
        - credentials-binding:
            - file:
                credential-id: maven-release-user-netrc
                variable:      NETRC_FILE
        # Credentials for Archiva
        - config-file-provider:
            files:
                -   file-id: 'org.jenkinsci.plugins.configfiles.maven.MavenSettingsConfig.ArchivaCredentialsSettings'
                    target:  archiva-credentials.xml
        - timeout:
            timeout: 45
            fail:    true
        - timestamps
    builders:
        - docker-castor-load
        - docker-log-dir
        # The config-file-provider has the file created by the jenkins-slave and
        # respecting umask. It is thus not readable by the nobody user in the
        # container. setfacl lets us grant an extra permission.
        # Credentials binding makes it readable solely by the jenkins-slave user.
        - shell: |
                 setfacl -m user:65534:r archiva-credentials.xml $NETRC_FILE
        - docker-run-with-log-and-workspace-cache:
            options: >-
                     -v "$NETRC_FILE":/nonexistent/.netrc:ro
                     -v "$WORKSPACE/archiva-credentials.xml":/archiva-credentials.xml
                     -e GIT_AUTHOR_NAME="maven-release-user"
                     -e GIT_COMMITTER_NAME="maven-release-user"
                     -e EMAIL="maven-release-user@wikimedia.org"
            image:   docker-registry.wikimedia.org/releng/maven-java8:1.0.0
            args:    >-
                     -Dresume=false
                     -Djdk.net.URLClassPath.disableClassPathURLCheck=true
                     -Duser.name='maven-release-user'
                     -DdeveloperConnection="scm:git:$ZUUL_URL/$ZUUL_PROJECT"
                     --settings /archiva-credentials.xml
                     release:prepare release:perform -Darguments=-Dgpg.skip=true
    publishers: '{publishers}'

- job-template:
    name: '{name}-{project}-maven-release-docker'
    !!merge : *job_template_maven-release-docker

# Node.js 10 job for 'npm install-test'
- job-template:
    name: '{name}-node10-docker'
    node: Docker
    triggers:
     - zuul
    properties:
        - build-discarder:
            days-to-keep: 7
    builders:
     - docker-castor-load
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10-test:0.6.1-s2'
    publishers:
     - archive-log-allow-empty
     - castor-save-workspace-cache
     - docker-cleanup

# Node.js 10 job for 'npm install & npm run {script}'
- job-template:
    name: '{name}-node10-run{script}-docker'
    node: Docker
    triggers:
     - zuul
    builders:
     - docker-castor-load
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10-test:0.6.1-s2'
        args: '{script}'
    publishers:
     - archive-log-allow-empty
     - castor-save-workspace-cache
     - docker-cleanup

- job-template:
    name: '{name}-node10-npmaudit-docker'
    node: Docker
    triggers:
     - zuul
    builders:
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10:0.5.2-s2'
        options: '--workdir="/src"'
        args: 'audit'
    publishers:
     - docker-cleanup

- job-template:
    name: '{name}-daily-node10-npmaudit-docker'
    node: Docker
    triggers:
    - timed: '@daily'
    parameters:
        - string:
              name: 'ZUUL_PROJECT'
              default: '{repository}'
        - string:
              name: 'ZUUL_REF'
              default: 'master'
        - string:
              name: 'ZUUL_URL'
              default: 'https://gerrit.wikimedia.org/r/p/'
        - string:
              name: 'NPM_CONFIG_cache'
              default: '/cache'
    builders:
    - docker-cache-dir
    - docker-src-dir
    - docker-log-dir
    - docker-ci-src-setup-simple
    - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10:0.5.2-s2'
        options: '--workdir="/src"'
        args: 'audit'
    publishers:
    - docker-cleanup

# Node.js 10 job for 'npm install-test' (with browsers installed)
- job-template:
    name: '{name}-node10-browser-docker'
    node: Docker
    triggers:
     - zuul
    builders:
     - docker-castor-load
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10-test-browser:0.6.3'
        options: '--shm-size 1g'
    publishers:
     - archive-log-allow-empty
     - castor-save-workspace-cache
     - docker-cleanup

# Node.js 10 job for 'npm install-test' (with browsers installed) and webdriver running
- job-template:
    name: '{name}-node10-browser-webdriver-docker'
    node: Docker
    triggers:
     - zuul
    builders:
     - docker-castor-load
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10-test-browser:0.6.3'
        options: '--entrypoint=/run-with-xvfb.sh --shm-size 1g'
    publishers:
     - archive-log-allow-empty
     - castor-save-workspace-cache
     - docker-cleanup

# Node.js 10 job for 'npm install-test' (with browser installed)
# and publishing /coverage from workspace
# to doc.wikimedia.org/cover/:DOC_PROJECT
- job:
    name: 'generic-node10-browser-coverage-docker-publish'
    node: Docker
    triggers:
     - zuul
    builders:
     - docker-castor-load
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10-test-browser:0.6.3-s2'
     - cover-publish:
        src: 'src/coverage'
        dest: '$DOC_PROJECT'
    publishers:
     - docker-cleanup

# Node.js 10 job for 'npm install & npm run doc'
# Publishing workspace/docs to doc.wikimedia.org/:DOC_PROJECT/:DOC_SUBPATH/
# - DOC_PROJECT: determined in parameter_functions.py (repo name)
# - DOC_SUBPATH: determined in parameter_functions.py (branch or tag)
- job:
    name: generic-node10-docs-docker-publish
    node: Docker
    triggers:
     - zuul
    builders:
     - docker-castor-load
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10-test:0.6.1-s2'
        args: 'doc'
     - doc-publish:
        docsrc: 'src/docs'
        docdest: '$DOC_PROJECT/$DOC_SUBPATH'
    publishers:
     - castor-save-workspace-cache
     - docker-cleanup
# Publishing workspace/docs to doc.wikimedia.org/:DOC_PROJECT/:DOC_SUBPATH/js/
# - DOC_PROJECT: determined in parameter_functions.py (mw extension name)
# - DOC_SUBPATH: determined in parameter_functions.py (branch or tag)
- job:
    name: mwext-node10-docs-docker-publish
    node: Docker
    triggers:
     - zuul
    builders:
     - docker-castor-load
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        image: 'docker-registry.wikimedia.org/releng/node10-test:0.6.1-s2'
        args: 'doc'
     - doc-publish:
        docsrc: 'src/docs'
        docdest: '$DOC_PROJECT/$DOC_SUBPATH/js'
    publishers:
     - castor-save-workspace-cache
     - docker-cleanup

- project:
    name: 'generic'
    jobs:
        - '{name}-node10-docker'
        - '{name}-node10-browser-docker'
        - '{name}-node10-browser-webdriver-docker'
        - '{name}-node10-npmaudit-docker'
        - '{name}-node10-run{script}-docker':
           script: 'doc'

# Same as 'generic', but kept separate so that gate-and-submit jobs
# don't cross-block between mediawiki/* and non-mediawiki changes.
- project:
    name: 'mwext'
    jobs:
        - '{name}-node10-run{script}-docker':
           script: 'doc'

- job-template:
    name: 'selenium-daily-{sitename}-{project}'
    node: Docker
    docker_image_var: 'docker-registry.wikimedia.org/releng/node10-test-browser:0.6.3-s2'
    triggers:
     - timed: '@daily'
    parameters:
        - string:
            name: 'MEDIAWIKI_USER'
            default: 'Selenium user'
        - string:
            name: 'MW_SERVER'
            default: '{mw_server}'
        - string:
            name: 'ZUUL_BRANCH'
            default: 'master'
        - string:
            name: 'ZUUL_PIPELINE'
            default: 'postmerge'  # to force saving npm cache with castor
        - string:
            name: 'ZUUL_PROJECT'
            default: '{repository}'
        - string:
            name: 'ZUUL_REF'
            default: 'master'
        - string:
            name: 'ZUUL_URL'
            default: 'https://gerrit.wikimedia.org/r/p/'
    builders:
     - docker-castor-load
     - docker-src-dir
     - docker-log-dir
     - docker-ci-src-setup-simple
     - docker-run-with-log-cache-src:
        options: '--env LOG_DIR=/log --entrypoint=/run-with-xvfb.sh'
        image: '{obj:docker_image_var}'
        args: 'selenium-daily'
    wrappers:
      - timeout:
          timeout: 30
      - timestamps
      # Wiki usernames and passwords are hold in Jenkins credentials store
      # https://integration.wikimedia.org/ci/credential-store/domain/selenium/
      - credentials-binding:
          - text:
              credential-id: selenium-user-beta
              variable: MEDIAWIKI_PASSWORD
          - text:
              credential-id: composer-github-oauthtoken
              variable: COMPOSER_GITHUB_OAUTHTOKEN
    publishers:
     - archive-log-allow-empty
     - castor-save-workspace-cache
     - junit:
         results: 'log/junit*.xml,log/WDIO.xunit*.xml'
         # We do not have any JUnit files when all tests pass
         allow-empty-results: true
     - email-ext:
        recipients: '{recipients}'
        body: '${{SCRIPT, template="wikimedia.template"}}'
        failure: true
        unstable: true
     - docker-cleanup
