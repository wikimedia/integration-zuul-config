- project:
    name: 'wikimedia-wikimania-scholarships'
    jobs:
     - '{name}-jslint'
     - '{name}-phpunit'
- project:
    name: 'wikimedia-iegreview'
    jobs:
     - '{name}-jslint'
     - '{name}-phpunit'
