- job-template:
    name: 'php55-{name}-build'
    node: contintLabsSlave && UbuntuTrusty
    defaults: use-remoteonly-zuul
    triggers:
     - zuul
    builders:
     - shell: |
        /usr/bin/phpize5
        ./configure
        make
        make test

- job-template:
    name: 'php55-{name}-hhvm-build'
    node: contintLabsSlave && UbuntuTrusty
    defaults: use-remoteonly-zuul
    triggers:
     - zuul
    builders:
     - shell: |
         hphpize
         cmake .
         make

# FastStringSearch PHP extension
- project:
    name: 'FastStringSearch'
    jobs:
     - 'php55-{name}-build'
     - 'php55-{name}-hhvm-build'

# NativePreprocessor PHP extension
- project:
    name: 'NativePreprocessor'
    jobs:
     - 'php55-{name}-build'

# luasandbox PHP extension
- project:
    name: 'luasandbox'
    jobs:
     - 'php55-{name}-build'
     - 'php55-{name}-hhvm-build'

# wikidiff PHP extension
- project:
    name: 'wikidiff'
    jobs:
     - 'php55-{name}-build'

# wikidiff2 PHP extension
- project:
    name: 'wikidiff2'
    jobs:
     - 'php55-{name}-build'
     - 'php55-{name}-hhvm-build'

# wmerrors PHP extension
- project:
    name: 'wmerrors'
    jobs:
     - 'php55-{name}-build'
