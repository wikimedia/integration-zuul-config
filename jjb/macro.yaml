- publisher:
    name: archive-log-dir
    publishers:
     - archive:
        artifacts: 'log/'

- publisher:
    name: archive-log-allow-empty
    publishers:
     - archive:
        artifacts: 'log/'
        allow-empty: true
