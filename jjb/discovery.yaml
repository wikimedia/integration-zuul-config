- project:
    name: discovery
    project:
        - parent-pom
        - maven-tool-configs
    jobs:
        - '{name}-{project}-maven-java8-docker'
        - '{name}-{project}-maven-java8-docker-site-publish'
