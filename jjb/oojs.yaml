- project:
    name: oojs-ui
    jobs:
        - '{name}-npm-node-4.3'
        - '{name}-npm-run-{script}-node-4.3':
            script:
                - doc
                - demos
        - '{name}-npm-run-{script}':
            script:
                - doc
                - demos
        - '{name}-rake-jessie'
        - '{name}-jshint'
        - '{name}-jsonlint'

- project:
    name: oojs-core
    jobs:
        - '{name}-npm-run-{script}-node-4.3':
            script:
                - doc
        - '{name}-npm-run-{script}':
            script:
                - doc
