- project:
    name: oojs-ui
    jobs:
        - '{name}-npm-node-4.3'
        - '{name}-npm-run-{script}-node-4.3':
            script:
                - doc
                - demos
        - '{name}-rake-jessie'
        - '{name}-jshint'
        - '{name}-jsonlint'
        - '{name}-composer-package-hhvm-trusty'
        - '{name}-composer-package-php55-trusty' 

- project:
    name: oojs-core
    jobs:
        - '{name}-jsduck-jessie'
