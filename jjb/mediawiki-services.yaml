- project:
    # mediawiki/services/cxserver.git
    name: 'cxserver'
    jobs:
     - '{name}-npm'

- project:
    # mediawiki/services/rcstream.git
    name: 'rcstream'
    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    # mediawiki/services/jobrunner.git
    name: 'jobrunner'
    jobs:
     - '{name}-phplint'
