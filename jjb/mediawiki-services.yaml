- project:
    name: 'change-propagation'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-6-jessie'

- project:
    name: 'citoid'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-6-jessie'

- project:
    # mediawiki/services/cxserver.git
    name: 'cxserver'
    repository:
     - deploy  # mediawiki/services/cxserver/deploy
    jobs:
     - '{name}-{repository}-npm-node-6-jessie'

- project:
    name: 'graphoid'
    repository:
     - deploy
    jobs:
     - '{name}-npm-node-6-docker':
         docker_image_var: wmfreleng/npm-test-graphoid:v2017.12.08.16.14
     - '{name}-{repository}-npm-node-6-jessie'

- project:
    name: 'kartotherian'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-6-jessie'

- project:
    name: 'mathoid'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-6-jessie'

- project:
    name: 'mobileapps'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-6-jessie'

- project:
    name: 'tilerator'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-6-jessie'

- project:
    name: 'trending-edits'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-6-jessie'
