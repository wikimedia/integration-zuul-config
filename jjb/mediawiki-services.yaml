- project:
    name: 'citoid'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-4'

- project:
    # mediawiki/services/cxserver.git
    name: 'cxserver'
    repository:
     - deploy  # mediawiki/services/cxserver/deploy
    jobs:
     - '{name}-{repository}-npm-node-4'

- project:
    name: 'graphoid'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-4'

- project:
    name: 'mathoid'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-4'

- project:
    name: 'mobileapps'
    repository:
     - deploy
    jobs:
     - '{name}-{repository}-npm-node-4'
