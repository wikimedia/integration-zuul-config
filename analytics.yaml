# Jobs for git repositories under analytics/*

- project:
    name: 'analytics-asana-stats'
    gerrit-name: 'analytics/asana-stats'

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    name: 'analytics-check-stats'
    gerrit-name: 'analytics/check-stats'

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    name: 'analytics-editor-geocoding'
    gerrit-name: 'analytics/editor-geocoding'

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    name: 'analytics-gerrit-stats'
    gerrit-name: 'analytics/gerrit-stats'

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    name: 'analytics-glass'
    gerrit-name: 'analytics/glass'

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    name: 'analytics-reportcard'
    gerrit-name: 'analytics/reportcard'

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'
