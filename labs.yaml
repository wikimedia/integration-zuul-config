# Holds various projects under 'labs/'

- project:
    name: 'labs-incubator'
    jobs:
     - '{name}-phplint'

- project:
    name: 'labs-nagios-builder'
    jobs:
     - python-jobs

- project:
    name: 'labs-tools-grrrit'
    jobs:
     - '{name}-jslint'
     - '{name}-yamllint'
