FROM {{ "ci-stretch" | image_tag }}

USER root

RUN {{ "helm rake kubeyaml envoyproxy" | apt_install }} && \
    mkdir -p /etc/envoy/ssl && chmod -R 777 /etc/envoy

USER nobody

WORKDIR /src
ENTRYPOINT ["rake"]
