FROM {{ "composer" | image_tag }}

User root

# Create directory where we're going to install to
RUN mkdir -p /opt/phan \
    && chown nobody:nogroup /opt/phan


USER nobody

COPY run.sh /run.sh
ENTRYPOINT ["/run.sh"]
