- project:
    name: 'mw-tools-codeutils'
    gerrit-name: 'mediawiki/tools/code-utils'

    jobs:
     - '{name}-pep8'
     - '{name}-phplint'

- project:
    name: 'mw-tools-release'
    gerrit-name: 'mediawiki/tools/release'

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'
