- project:
    name: 'mw-tools-codeutils'

    jobs:
     - '{name}-pep8'
     - '{name}-phplint'

- project:
    name: 'mw-tools-dippybird'

    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-tools-fluoride'

    jobs:
     - '{name}-pep8'
     - '{name}-pyflakes'

- project:
    name: 'mw-tools-release'

    jobs:
     - '{name}-pep8'
     - '{name}-phplint'
     - '{name}-pyflakes'
