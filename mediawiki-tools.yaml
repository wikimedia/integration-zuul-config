- project:
    name: 'mw-tools-codeutils'

    jobs:
     - '{name}-phplint'
     - python-jobs

- project:
    name: 'mw-tools-dippybird'

    jobs:
     - '{name}-phplint'

- project:
    name: 'mw-tools-fluoride'

    jobs:
     - python-jobs

- project:
    name: 'mw-tools-release'

    jobs:
     - '{name}-phplint'
     - python-jobs

- project:
    name: 'mw-tools-scap'
    toxenv:
     - py27
     - flake8
    jobs:
     - '{name}-tox-{toxenv}'
     - python-jobs
